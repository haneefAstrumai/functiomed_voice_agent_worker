# ── LiveKit Agent core ────────────────────────────────────────
livekit-agents[groq,openai,deepgram,silero,turn-detector]>=1.2.0

# ── LiveKit plugins (explicit pins for stability) ─────────────
livekit-plugins-groq>=1.0.0
livekit-plugins-openai>=1.0.0
livekit-plugins-deepgram>=1.0.0
livekit-plugins-silero>=1.0.0
livekit-plugins-turn-detector>=1.0.0

# ── HTTP client ───────────────────────────────────────────────
httpx>=0.27.0

# ── Env + utils ───────────────────────────────────────────────
python-dotenv>=1.0.0
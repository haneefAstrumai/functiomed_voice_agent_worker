# ── LiveKit Agent core ────────────────────────────────────────
livekit-agents[groq,openai,deepgram,silero,turn-detector]>=0.12.0

# ── LiveKit plugins (explicit pins for stability) ─────────────
livekit-plugins-groq
livekit-plugins-openai
livekit-plugins-deepgram
livekit-plugins-silero
livekit-plugins-turn-detector

# ── HTTP client (rag_client.py → RAG backend calls) ──────────
httpx>=0.27.0

# ── Env + utils ───────────────────────────────────────────────
python-dotenv>=1.0.0